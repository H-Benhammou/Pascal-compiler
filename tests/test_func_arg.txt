program TestFuncArgs;

var
  x, y, rGlobal: Integer;

function Sum(a, b: Integer): Integer;
begin
  Sum := a + b;
end;

begin
  read(x);
  read(y);

  rGlobal := Sum(x, y);

  write(rGlobal);
end.
